[project]
name = "flickstream"
version = "0.1.0"
description = "A Flask-based movie watchlist application with TMDb integration"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.0",
    "requests>=2.31.0",
    "gunicorn>=21.2.0",
]

